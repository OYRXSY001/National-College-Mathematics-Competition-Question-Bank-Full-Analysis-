# 使用必读（收件人请先看这里）

> **出品**：OYRXSY001 · 转载请保留出处

这是一个**开箱即用**的数学竞赛真题题库（第 1–17 届，39 卷 / 254 题，全部题目均含详细解析；其中第 14–17 届附带原版试卷 PDF）。你不需要改任何代码，按下面两步即可在本机运行。

## 方式 A（最省事）：直接双击 `数学竞赛题库.exe`

Windows 上**不需要安装 Python**，双击即启动：先出现一个**品牌启动画面**（几秒），随后自动弹出浏览器进入登录页（弹窗提示“Windows 已保护你的电脑”时点 **更多信息 → 仍要运行** 即可，程序没有数字签名）。

- 启动画面期间程序在后台解压并准备题库，**不会弹出黑色终端窗口**；所有启动输出写入数据目录里的 `bank.log`（出问题时可查看/发给技术支持）。
- **数据存放位置**：默认固定在 `%LOCALAPPDATA%\MathQuestionBank`（把资源管理器地址栏粘贴进去即可打开）。**exe 放在哪、挪到哪里都无所谓**，数据始终在同一个地方；直接从压缩包里双击运行也不会丢数据。网页「我的」页右下角的 **📂 打开数据文件夹** 按钮可一键直达。
- 备份/转移数据 = 拷贝 `%LOCALAPPDATA%\MathQuestionBank` 这个文件夹（数据库、配图、日志都在里面）。
- 想让数据跟着 exe 走（U 盘便携场景）：在 exe 旁边新建一个空文件命名为 `portable.flag`，数据就改存 exe 同目录。
- 老版本把数据放在 exe 同目录：新版首次启动会自动把旧数据搬到默认位置（原文件改名留档为 `db.sqlite3.migrated.bak`，确认无虞后可删）。
- 再次双击 exe 不会开第二个服务，而是直接把浏览器带到正在运行的那个实例。
- **退出程序**：网页里点「我的 → 退出桌面版」，服务随页面提示停止，数据保留；也可在任务管理器中结束 `数学竞赛题库.exe`。
- 运行中不要随手删除 `.bank.lock`（单实例锁，退出时会自动清理；异常退出残留也不影响下次启动）。

## 方式二（备选）：`start_bank_site.bat`（需要 Python）

## 你需要准备什么

- 一台 Windows 电脑
- **Python 3.11 或更新版本**（唯一依赖）
  - 下载：<https://www.python.org/downloads/>
  - 安装时务必勾选 **“Add python.exe to PATH”**
- 网络连接（仅首次运行时自动下载 Python 库用一次）

不需要 Node.js、npm、数据库软件——前端资源和题库数据都已内置。

## 第一步：双击 `start_bank_site.bat`

首次运行会自动完成：创建虚拟环境 → 安装依赖 → 初始化数据库，然后自动打开浏览器**进入登录页**。

以后每次使用直接再双击它即可（秒级启动）。

## 第二步：进入系统（登录 / 注册）

网站打开后先停在**登录页**，浏览全部题目需要登录：

- **没账号**：登录页点“立即注册”，填用户名和密码即可（注册后自动登录，全程无需邮箱）。
- **已有账号**：直接输入用户名密码登录。
- **一个账号多台电脑/多人共用**：分享给同事即可，账号不绑定机器。

## 第三步（可选）：创建管理员账号

双击 `create_admin_account.bat`，按提示输入用户名和密码。之后访问 <http://127.0.0.1:8000/admin/> 进入后台，可用于增删题目、上传试卷 PDF。普通注册的账号没有后台权限，不影响前台浏览与收藏/错题本功能。

## 登录后能做什么

| 功能 | 入口 | 说明 |
|---|---|---|
| 真题浏览 / 搜索 / 筛选 | 首页、真题库 | 按届数、初赛/决赛、题型、知识点筛选 |
| 收藏 / 错题本 | 顶部导航 | 收藏好题；答错的题自动进错题本（累计次数、最近答错时间，可标记已掌握） |
| 练习模式 | 试卷页“连续刷本卷” / 错题本“重新练习” / 我的“开始刷题” | 一题一屏，对错一键判定；答错自动记入错题本；支持快捷键 **1 答对 / 2 答错 / 3 跳过、回车下一题**；中途退出下次自动**从上次进度继续** |
| 间隔复习 | 首页“今日任务” / 错题本“今日复习” | 答错的题**次日**进入复习；复习答对按 1→3→7→15 天拉长间隔，连对 4 次自动“已掌握”；错题卡片显示“⏰ 今天要复习 / 🔁 下次复习日期” |
| 智能组卷 | 首页“今日任务” / 我的“智能组卷” | 从你答错最多的 Top3 知识点随机抽 10–15 题，专攻薄弱环节 |
| 模拟考试 | 试卷详情页“⏱ 模拟考试” | 整卷按 120 分钟倒计时作答（到时自动交卷），每题自评“答对/答错/未作答”；交卷出成绩单：总分、正确率、按题型与知识点得分率，每题可展开答案与解析；“再考一次”或“按本卷逐题练习” |
| 每日一题 | 首页 | 每天一道全站相同的题，点“去做这一题”直接练 |
| 我的（统计） | 顶部导航“我的” | 累计作答、正确率、近 7 天 / 近 30 天做题分布（点右上角切换）、薄弱知识点、最近一次模拟考试的分数 |
| 个人笔记 | 题目详情页底部 | 每道题可写一条笔记（支持 Markdown 与公式），“我的 → 我的笔记”汇总查看 |
| 整卷打印 | 试卷详情页“打印整卷” | 可先打不含答案的作答版，再打带答案解析版；浏览器打印/存 PDF |
| 数据导出 | “我的”页底部 | 把作答记录、收藏、错题、笔记导出一个 CSV，Excel 可直接打开 |

## 常见问题

| 问题 | 处理 |
|---|---|
| 双击 exe 弹出蓝色“Windows 已保护你的电脑” | 点 **“更多信息” → “仍要运行”**（程序没有数字签名，属正常现象） |
| 双击 bat 时弹出蓝色“Windows 已保护你的电脑” | 点 **“更多信息” → “仍要运行”**（脚本没有数字签名，属正常现象） |
| 双击 exe 没反应/浏览器没弹出 | 看数据目录（`%LOCALAPPDATA%\MathQuestionBank`，或「我的 → 📂 打开数据文件夹」直达）里的 `bank.log` 最后几行；常见原因是 8000–8009 端口都被占用（日志会说明），或杀毒软件拦截 |
| 如何退出桌面版 | 网页右上角「我的」→ 右下角「退出桌面版」（数据保留，下次再打开继续）；或任务管理器结束 `数学竞赛题库.exe` |
| **我的练习数据会丢吗？** | **不会**。账号、作答、错题、练习进度都存在数据目录（默认 `%LOCALAPPDATA%\MathQuestionBank`）的 `db.sqlite3` 里，每次启动读写**同一个文件**——关掉再开、重启电脑、挪动甚至删除 exe 后数据**原样恢复**（启动日志出现”检测到已有数据库，将保留原有记录”）。只有当这个文件被删掉时才会回到初始题库 |
| 换电脑 / 换文件夹 / 软件升级换新 exe | 把旧目录里的 **`db.sqlite3` 文件 + `media/` 文件夹**一起复制到新 exe 所在目录，数据全部跟随；不同步复制则从全新题库开始 |
| exe 所在目录删掉了 db.sqlite3 | 重新双击 exe，会再次从内置题库初始化（旧账号和练习记录会丢失，请按上一条迁移/备份） |
| 首次安装依赖很慢/失败 | 检查网络；已默认使用清华 PyPI 镜像 |
| 想换端口 | 编辑 `start_bank_site.bat`，把其中所有 `8000` 改成别的数字；exe 版在 8000–8009 自动挑一个空闲端口 |
| 局域网内让其他人访问 | 把启动脚本里 `127.0.0.1:8000` 改成 `0.0.0.0:8000`，他人访问 `http://你的IP:8000`。**仅建议在信任的网络内这样做** |
| 数据备份 | 复制 `db.sqlite3` 和整个 `media/` 文件夹即可 |

## 打包者备忘（不会出现在发行包里的说明）

- 制作发行包：项目根目录执行 `powershell -ExecutionPolicy Bypass -File make_release.ps1`，产物在桌面。
- 制作单文件 exe：项目根目录执行 `.venv\Scripts\pyinstaller --noconfirm --clean 数学竞赛题库.spec`（新装机器先 `pip install pyinstaller`）。spec 已配置：无控制台窗口（console=False）、启动画面 `packaging_assets/splash.png`（想换图就替换该文件重新打包，注意尺寸 ≤760×480 且不要用纯品红 #ff00ff）。
- 开发机更新公式渲染/导入工作簿时才需要 Node.js 和 npm；站点本身不依赖。
